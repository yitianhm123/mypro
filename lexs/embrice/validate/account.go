package validate

// var _regex = regexp.MustCompile(`[a-zA-Z0-9][a-zA-Z0-9_@.]*$`)

// must: if ture len(input) > 1, false mean that the input is a optional value
func Account(in string, must bool) error {
	return nil
}

// must: if ture len(input) > 1, false mean that the input is a optional value
func Password(in string, must bool) error {
	return nil
}

// must: if ture len(input) > 1, false mean that the input is a optional value
func NickName(in string, must bool) error {
	return nil
}

// must: if ture len(input) > 1, false mean that the input is a optional value
func Mobile(in string, must bool) error {
	return nil
}

// must: if ture len(input) > 1, false mean that the input is a optional value
func Address(in string, must bool) error {
	return nil
}

// must: if ture len(input) > 1, false mean that the input is a optional value
func Email(in string, must bool) error {
	return nil
}
